#include <stdio.h>

int main(){
    int a,b;
    scanf("%d%d",&a,&b);
    if(a>b)
    {
        printf("%d is bigger ",a);
    }
    if(b>a)
    {
        printf("%d is bigger ",b);
    }
    return 0;
}