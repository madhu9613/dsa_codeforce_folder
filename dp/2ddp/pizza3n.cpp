#include<bits/stdc++.h>
using namespace std;
int solve(int index,int ending,vector<int> slice,int n)
{
    if()
}
int main()
{
    int k;cin>>k;
    vector<int> slice(k);
    for(int i=0;i<k;i++)
    {
        cin>>slice[i];
    }

    return 0;
}